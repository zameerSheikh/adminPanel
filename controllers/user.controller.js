(function(){
    "use strict";

    adminApp.controller('UsersController', usersController);

    usersController.$inject = [
        '$scope',
        '$timeout'
    ];

    function usersController(
        $scope,
        $timeout
    ){

    }
    
}())